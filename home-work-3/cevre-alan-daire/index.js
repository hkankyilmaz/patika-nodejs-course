const circleArea = (r) => {
  return console.log(Math.PI * r * r);
};

const circleCircumference = (r) => {
  return console.log(2 * Math.PI * r);
};

module.exports = {
  circleArea,
  circleCircumference,
};
